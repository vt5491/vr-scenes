/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Vrscene} from './vrscene';

describe('Vrscene', () => {
  it('should create an instance', () => {
    expect(new Vrscene()).toBeTruthy();
  });
});
